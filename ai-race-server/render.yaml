services:
  - type: web
    name: ai-race
    runtime: python
    plan: free
    pythonVersion: "3.12"
    buildCommand: pip install -r requirements-play.txt
    startCommand: python serve_play.py --host 0.0.0.0 --port $PORT
